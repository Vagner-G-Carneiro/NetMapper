# NetMapper
NetMapper é uma aplicação web que permite ao usuário medir a velocidade de sua conexão com a internet (download, upload, ping e jitter) diretamente pelo navegador, sem depender de APIs externas pagas.
